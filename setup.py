# -*- coding: utf-8 -*-

# Minimal setup.py that defers to pyproject.toml
# This is kept for backward compatibility with buildout

from setuptools import setup

# All configuration is now in pyproject.toml
setup()
